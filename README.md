# envirohome_simulator
Simulator module for Envirohome


Build instructions:

1. project root directory
`$ docker-compose build`


Run instructions:

1. project root directory
`$ docker-compose up -d`
